<%= form_with(model: player_event, local: true) do |form| %>
  <% if player_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player_event.errors.count, "error") %> prohibited this player_event from being saved:</h2>

      <ul>
        <% player_event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :player_id %>
    <%= form.text_field :player_id %>
  </div>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.text_field :event_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
